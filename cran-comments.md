## Resubmission
This is a resubmission. In this version I have:

* Changed the example for `run_sims.R`, which was previously wrapped in `if(interactive()){}` to be inside `\donttest{}`

* Fixed a missing \value tag in the documentation file for `plot_width_vs_calls()`

## R CMD check results

0 errors | 0 warnings | 0 notes

* This is a new release.
